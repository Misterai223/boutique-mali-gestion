
export interface ThemeSettings {
  shopName: string;
  currency: string;
  darkMode: boolean;
  notifications: boolean;
  logoUrl: string;
  primaryColor: string;
  accentColor: string;
  secondaryColor: string;
  borderRadius: string;
  fontFamily: string;
}
